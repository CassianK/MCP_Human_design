# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs", "python3", "gcc"]

[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm rebuild swisseph"]

[start]
cmd = "npm start"
