# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs-20_x", "python3", "gcc"]
[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm rebuild swisseph"]

[start]
cmd = "npm start"

